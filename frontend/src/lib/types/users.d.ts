export interface Student {
  students_id: {
    id: string
    first_name: string
    last_name: string
  }
}

export interface Teacher {
  teachers_id: {
    id: string
    first_name: string
    last_name: string
  }
}
